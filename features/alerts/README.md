# features/alerts

Not yet populated. Reserved for feature-scoped logic (local hooks,
feature-specific components, and orchestration) once the alerts phase starts.
Until then, cross-cutting pieces (types, ui atoms, dashboard components,
services) live in their top-level folders — nothing should be duplicated
into here prematurely.
