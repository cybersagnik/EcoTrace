// Mirrors styles/variables.css — keep in sync. Used where JS/TS needs the
// value directly (SVG gradients, inline styles) rather than a CSS var.
export const CARBON_COLOR = {
  clean: "#4CAE8C",
  moderate: "#D9A441",
  high: "#C2512E",
} as const;

export const ACCENT_COLOR = "#5FA8C7";
