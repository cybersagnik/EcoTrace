// Phase-later. Project currently uses native fetch via lib/fetcher.ts.
// Swap in here if/when interceptors, auth-refresh-on-401, or request
// cancellation make an axios instance worth the dependency.
export {};
